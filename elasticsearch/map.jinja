{% set elasticsearch_map = salt['grains.filter_by']({
    'default': {
        'pkg': 'elasticsearch',
        'use_repo': True,
        'custom_repo_url': '',
        'custom_repo_gpgkey': '',
    },
},
merge=salt['pillar.get']('elasticsearch', {}),
default='default')
%}
